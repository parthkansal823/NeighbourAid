[pytest]
asyncio_mode = auto
pythonpath = .

# Treat warnings from our own code as errors so a deprecation (an async API
# changing under us, a datetime.utcnow() creeping back in) fails the build
# instead of scrolling past. Third-party noise stays a warning.
filterwarnings =
    default
    error::DeprecationWarning:app.*
