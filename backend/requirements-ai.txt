# Optional: local zero-shot triage model.
#
#   pip install -r requirements.txt -r requirements-ai.txt
#
# Installs ~2.5 GB of wheels, and the first triage call downloads a further
# ~1.6 GB model (facebook/bart-large-mnli). Only worth it on a host with
# >= 2 GB RAM that you actually want running inference locally.
#
# WITHOUT this file the app still works. app/services/ai.py catches the
# missing import and falls back to a keyword heuristic covering English,
# Hindi (Devanagari) and Hinglish, which classifies urgency, vulnerability
# and time-sensitivity, and reports the matched terms as `triggers`. The
# fallback is what CI and the test suite exercise, so it stays honest.
#
# To force the fallback even when these are installed (small VM, offline
# dev, deterministic tests), set NA_DISABLE_AI_MODEL=1.
#
# Floors clear known CVEs — torch < 2.13.0 has an open advisory.
transformers>=4.46.0
torch>=2.13.0
